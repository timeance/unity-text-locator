interface:
  display_name: "Unity Translation Pipeline"
  short_description: "Safely extract, translate, write, and debug Unity game text."
  default_prompt: "Use $unity-text-locator to inspect this Unity game, extract and validate project-named translation CSVs, choose the correct writeback adapter, inspect TMP font-bundle compatibility and glyph coverage, and verify runtime loading before packaging."

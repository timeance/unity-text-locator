interface:
  display_name: "Unity Translation Pipeline"
  short_description: "Safely extract and localize Unity text and fonts."
  default_prompt: "Use $unity-text-locator to discover Unity text sources, validate occurrence-mapped translations, choose a safe static or optional runtime adapter, and require a runtime canary before packaging."
